<div>
  <h1 class=" font-extrabold text-3xl my-4">お知らせ</h1>
  <% @posts.each do |post| %>
  <div class="py-5 px-2 mx-40 shadow-md">
    <h2 class=" font-extrabold text-3xl my-4">title:<%=post.title%></h2>
    <p><%=post.content%></p>
    <%= link_to "edit", edit_admin_post_path(post.id), class:"px-3" %>
  </div>
  <% end %>
</div>